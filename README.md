Point -> define point on screen, move it in (using sleep()) a certain direction (Xdiff,Ydiff)

Snake -> creates an array of points and moves them (Xdiff,Ydiff)

SnakeWithKeyboardAnd Walls -> Creates a defined game board (can be used for the status spaces for characters), when char hits a wall it stoppes moving

